# docStamp production environment variables
# Copy to /etc/docstamp/env.conf and fill in the values.

# AES-256 key for encrypting sensitive document properties (64 hex chars)
DOCSTAMP_ENCRYPTION_KEY=

# DeepSeek API key for AI features (text correction, format detection, denoise).
# Leave empty to disable AI features — endpoints will return unchanged input.
DOCSTAMP_DEEPSEEK_API_KEY=

# Celery broker — Redis (production default)
CELERY_BROKER_URL=redis://127.0.0.1:6379/0
CELERY_RESULT_BACKEND=redis://127.0.0.1:6379/1

# CORS — comma-separated list of allowed origins
DOCSTAMP_CORS_ORIGINS=https://your-domain.com

# Static files directory (Nuxt generate output)
DOCSTAMP_STATIC=/opt/docstamp/frontend/.output/public

# Upload storage directory
DOCSTAMP_UPLOAD_FOLDER=/opt/docstamp/output

# Log directory
DOCSTAMP_LOG_DIR=/var/log/docstamp
DOCSTAMP_LOG_LEVEL=20
